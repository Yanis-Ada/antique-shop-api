{
  "compilerOptions": {
    "target": "ES2020",                    // Version JavaScript cible
    "module": "commonjs",                  // Système de modules (Node.js standard)
    "lib": ["ES2020"],                     // Bibliothèques TypeScript disponibles
    "outDir": "./dist",                    // Dossier de sortie après compilation
    "rootDir": "./src",                    // Dossier source
    "esModuleInterop": true,               // Compatibilité imports ES6/CommonJS
    "allowSyntheticDefaultImports": true,  // Permet les imports par défaut
    "strict": true,                        // Mode strict (recommandé)             
    "skipLibCheck": true,                  // Accélère la compilation
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,             // Permet d'importer des fichiers JSON
    "declaration": true,                   // Génère les fichiers .d.ts
    "sourceMap": true,                     // Pour le debugging
    "removeComments": true,                // Retire les commentaires du code compilé
    "noImplicitAny": true,                 // Interdit les types 'any' implicites
    "noImplicitReturns": true,             // Toutes les branches doivent retourner quelque chose
    "noUnusedLocals": true,                // Détecte les variables non utilisées
    "noUnusedParameters": true             // Détecte les paramètres non utilisés
  },
  "include": [
    "src/**/*"                             // Inclut tous les fichiers dans src/
  ],
  "exclude": [
    "node_modules",                        // Exclut node_modules
    "dist"                                 // Exclut le dossier de compilation
  ]
}